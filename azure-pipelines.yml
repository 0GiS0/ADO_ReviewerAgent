# Pipeline Simplificada - Obtener Diferencias de PR
trigger: none

pr:
  branches:
    include:
      - main
      - develop
      - feature/*

pool:
  vmImage: ubuntu-latest

variables:
  - name: DIFF_OUTPUT
    value: "$(Build.ArtifactStagingDirectory)/pr-diff.txt"

steps:
  - script: echo "🚀 Iniciando pipeline para obtener diferencias del PR"
    displayName: 🚀 Inicio

  - bash: |
      echo "📋 Información del Pull Request:"
      echo "  - Repository URI: $(System.PullRequest.SourceRepositoryUri)"
      echo "  - PR #: $(System.PullRequest.PullRequestId)"
      echo "  - Source Branch: $(System.PullRequest.SourceBranch)"
      echo "  - Target Branch: $(System.PullRequest.TargetBranch)"
      echo "  - Source Commit: $(System.PullRequest.SourceCommitId)"
      echo "  - Build Repository: $(Build.Repository.Uri)"
      echo "  - Build Commit: $(Build.SourceVersion)"
      echo ""      
      
    displayName: 📋 Mostrar Información del PR

  - bash: |
      echo "🌐 Ejecutando script get-pr-diff.sh..."
      
      # Verificar que el PAT esté disponible
      if [ -z "$AZURE_DEVOPS_EXT_PAT" ]; then
        echo "❌ ERROR: AZURE_DEVOPS_EXT_PAT no está configurado"
        exit 1
      fi
      
      # Ejecutar el script directamente con los parámetros
      ./scripts/get-pr-diff.sh \
        "$(System.PullRequest.SourceRepositoryUri)" \
        "$(System.PullRequest.SourceBranch)" \
        "$(System.PullRequest.TargetBranch)" \
        "$AZURE_DEVOPS_EXT_PAT" \
        "$(DIFF_OUTPUT).json"
      
      if [ $? -ne 0 ]; then
        echo "❌ ERROR: El script get-pr-diff.sh falló"
        exit 1
      fi
      
    displayName: 🌐 Obtener Diferencias usando Azure DevOps API
    env:
      AZURE_DEVOPS_EXT_PAT: $(AZURE_DEVOPS_EXT_PAT)

  - bash: |
      echo "📝 Procesando respuesta para el pipeline..."
      
      # Verificar que el archivo JSON se haya creado
      if [ ! -f "$(DIFF_OUTPUT).json" ]; then
        echo "❌ ERROR: No se encontró el archivo de respuesta JSON"
        exit 1
      fi

      
      # Crear un resumen legible del diff
      echo "📊 Resumen del diff generado por Azure DevOps API" > "$(DIFF_OUTPUT)"
      echo "Fecha: $(date)" >> "$(DIFF_OUTPUT)"
      echo "PR #: $(System.PullRequest.PullRequestId)" >> "$(DIFF_OUTPUT)"
      echo "Source: $(System.PullRequest.SourceBranch) -> Target: $(System.PullRequest.TargetBranch)" >> "$(DIFF_OUTPUT)"
      echo "" >> "$(DIFF_OUTPUT)"
      
      # Agregar estadísticas si jq está disponible
      if command -v jq &> /dev/null && [ -f "$(DIFF_OUTPUT).json" ]; then
        if jq empty "$(DIFF_OUTPUT).json" 2>/dev/null; then
          echo "📊 Estadísticas de cambios:" >> "$(DIFF_OUTPUT)"
          echo "  - Total: $(jq '.changes | length' "$(DIFF_OUTPUT).json" 2>/dev/null || echo 'N/A')" >> "$(DIFF_OUTPUT)"
          echo "  - Añadidos: $(jq '.changeCounts.Add // 0' "$(DIFF_OUTPUT).json" 2>/dev/null || echo '0')" >> "$(DIFF_OUTPUT)"
          echo "  - Editados: $(jq '.changeCounts.Edit // 0' "$(DIFF_OUTPUT).json" 2>/dev/null || echo '0')" >> "$(DIFF_OUTPUT)"
          echo "  - Eliminados: $(jq '.changeCounts.Delete // 0' "$(DIFF_OUTPUT).json" 2>/dev/null || echo '0')" >> "$(DIFF_OUTPUT)"
          echo "" >> "$(DIFF_OUTPUT)"
          
          echo "📁 Archivos modificados:" >> "$(DIFF_OUTPUT)"
          jq -r '.changes[]?.item?.path // empty' "$(DIFF_OUTPUT).json" 2>/dev/null >> "$(DIFF_OUTPUT)"
          echo "" >> "$(DIFF_OUTPUT)"
          
          echo "🔍 Detalles de cambios:" >> "$(DIFF_OUTPUT)"
          jq -r '.changes[]? | "- \(.item.path // "unknown") (\(.changeType // "unknown"))"' "$(DIFF_OUTPUT).json" 2>/dev/null >> "$(DIFF_OUTPUT)"
        else
          echo "⚠️  JSON inválido - guardando respuesta completa" >> "$(DIFF_OUTPUT)"
          cat "$(DIFF_OUTPUT).json" >> "$(DIFF_OUTPUT)"
        fi
      else
        echo "⚠️  jq no disponible - guardando respuesta JSON" >> "$(DIFF_OUTPUT)"
        cat "$(DIFF_OUTPUT).json" >> "$(DIFF_OUTPUT)"
      fi
      
      echo "✅ Diff procesado y guardado en: $(DIFF_OUTPUT)"
      
    displayName: 📝 Procesar Respuesta de la API

  - bash: |
      echo "📄 Resumen del análisis usando API REST:"
      echo "  - Archivo JSON de la API: $(DIFF_OUTPUT).json"
      echo "  - Archivo de diferencias procesado: $(DIFF_OUTPUT)"
      
      if [ -f "$(DIFF_OUTPUT)" ]; then
        echo "  - Tamaño del diff: $(du -h "$(DIFF_OUTPUT)" | cut -f1)"
        echo "  - Líneas: $(wc -l < "$(DIFF_OUTPUT)")"
        echo "✅ Diff generado correctamente usando API REST"
        
        echo ""
        echo "📋 Primeras líneas del diff:"
        head -20 "$(DIFF_OUTPUT)"
      else
        echo "❌ ERROR: No se generó el archivo de diferencias"
        exit 1
      fi
      
      if [ -f "$(DIFF_OUTPUT).json" ]; then
        echo ""
        echo "📊 Tamaño de la respuesta JSON: $(du -h "$(DIFF_OUTPUT).json" | cut -f1)"
      fi
    displayName: 📄 Mostrar Resumen de API

  - task: PublishBuildArtifacts@1
    inputs:
      PathtoPublish: "$(Build.ArtifactStagingDirectory)"
      ArtifactName: "pr-diff"
      publishLocation: "Container"
    displayName: 📦 Publicar Diff como Artefacto

  - bash: |
      echo "✅ Pipeline completada exitosamente"
      echo "📁 El diff está disponible en los artefactos como 'pr-diff'"
    displayName: ✅ Completado


